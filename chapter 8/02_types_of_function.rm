'''two types of function are there :
1- built in functions:
                     already provided by the python
                     print()
                     len()
                     sum()
                     max()
                     min()
                     type()
                     range()

2- user defined function:
                         function that are created by the user
                         def square(n):
                              return n*2

                         print(square(2))   
'''