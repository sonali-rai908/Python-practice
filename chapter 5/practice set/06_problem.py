# can you change the value inside a list which is contained in set s
s={8,7,12,"harry",[1,2]} 
print(s) # it will raise an type error
# cant use list as a set element because lists are mutable (changeable) but set elements are immutable (unchangeable)

# in case of tuple ...a set can contain tuple as its element becuase they are als immutable but also you cant modify the tuple after creation

