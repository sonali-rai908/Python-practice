from maths_operation import add
# first it will execute all the code of file from where it is imported
# which sometimes we dont want to be
# when we import file we only want to use its methods , not the whole code

print(add(5,4))  # add running because we had imported it from maths_operation file


